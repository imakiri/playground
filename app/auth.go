package app

import (
	"github.com/imakiri/playground/data/schema"
)

func Hash(pass string) (re schema.Main) {
	//re.User.PassHash, re.Err = bcrypt.GenerateFromPassword([]byte(pass+salt), hashCost)
	return
}
